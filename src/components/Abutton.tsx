

const Abutton = ({ title }: { title: string }) => {
    return (
        <>
            <h2>Welcome to my first TypeScript react app</h2>
            <button>{title}</button>

        </>
    )
}

export default Abutton
